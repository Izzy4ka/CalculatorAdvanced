package com.example.calculator.domain.math.ast

enum class MathOperation {
    PLUS, MINUS, MULTIPLY, DIVIDE, POWER
}

enum class MathFunction { SIN, COS, TAN, LOG, LN, SQRT }

enum class MathConstant { PI, E }